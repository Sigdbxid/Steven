cmake_minimum_required(VERSION 3.15)
project(Fruit)
aux_source_directory(./ SRC)
include_directories(${SDL2_INCLUDE_DIRS} ${CMAKE_SOURCE_DIR}/include)
link_directories(${CMAKE_SOURCE_DIR}/lib ${SDL2_INCLUDE_DIRS})
link_libraries(Game)
set(LIBRARY_OUTPUT_PATH ${LIBPATH})
add_library(Fruit STATIC ${SRC})
target_include_directories(Fruit ${SDL2_INCLUDE_DIRS})
target_link_libraries(Fruit SDL2)